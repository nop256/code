#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "wordle.h"

struct vector *vector_new(void) {
    fprintf(stderr, "vector_new: not implemented\n");
    exit(1);
}

void vector_push(struct vector *v, char *str) {
    fprintf(stderr, "vector_push: not implemented\n");
    exit(1);
}

void vector_free(struct vector *v) {
    fprintf(stderr, "vector_free: not implemented\n");
    exit(1);
}
